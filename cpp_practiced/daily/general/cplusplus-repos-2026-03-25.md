# C++ Learning Repos — Evening Study Notes
**Date:** 2026-03-25 | **Time:** 11:30 PM CDT

---

## Repos Studied

- **3BuckyPrintOnScreen** — Simplest: prints text with `cout`
- **4CPPVariables** — Variables and basic data types
- **5CPPCreatingBasicCalculator** — Intro to arithmetic + input
- **6CPPBuckyVariableMemryConceptsWNotes** — Variable declaration, memory assignment, overwriting
- **6CPPBuckyVariableMemryConceptsWNotes** — Same topic with richer annotation
- **1.1ClaudeCPP** — Multi-lesson repo: functions → classes → control flow → loops
- **1.1Oppie1CPP** — Parallel version of the above, branded "Oppie1" instead of "Claude"
- **74.ExtraCred.VectorsWithNotes** — Advanced: vectors, likely from a college course

---

## C++ Concepts Being Taught (in order of progression)

1. **Printing to screen** — `cout << "text"`, escape sequences `\n`
2. **Variables & data types** — `int`, declaration vs. assignment
3. **Basic arithmetic** — addition, subtraction, calculators
4. **Variable memory mechanics** — one name, one memory location, overwrite the value
5. **Functions with multiple parameters** — `int addNums(int x, int y, int a, int b)`
6. **Classes & objects** — intro to OOP, what is a class vs. an object
7. **Variables inside classes** — getters/setters, accessing class members
8. **Constructors** — initializing objects with constructor functions
9. **If / if-else statements** — boolean logic in code
10. **While loops** — iteration fundamentals
11. **Vectors** — extra credit college-level work

---

## Tutorial Structure Patterns

### Single-concept repos (3, 4, 5, 6...)
Each repo is **one lesson = one Visual Studio project**:
```
RepoName/
├── RepoName.sln                          (Visual Studio solution file)
├── RepoName/                             (project folder)
│   ├── RepoName.vcxproj                 (project config)
│   └── main.cpp                          ← THE CODE
└── x64/Debug/                            (compiled output)
    └── RepoName.exe
```
- Very clean: one `.cpp` file, a single focused idea
- Compiled `.exe` is checked into the repo (showing it actually ran)
- Build artifacts (`vc143.idb`, `.ilk`, `.pdb`) also committed — shows the student builds and runs everything locally

### Multi-lesson repos (1.1ClaudeCPP, 1.1Oppie1CPP)
These are **course-style collections** — one repo with numbered sub-projects:
```
1.1ClaudeCPP/
├── 1.10ClaudeFuncWMultiplParmetrs/       ← Lesson 10
│   ├── main.cpp
│   └── ...
├── 1.11ClaudeIntroClassesNObjects/        ← Lesson 11
│   └── ...
├── 1.12Claude1VariablesInClassesGetNSet/  ← Lesson 12
│   └── ...
└── ... (up through at least 1.17)
```
- Numbering system suggests following a structured C++ course (likely Bucky Roberts' thenewboston YouTube series, based on the "Bucky" naming in the simpler repos)
- Each AI model (Claude, Grok, Gemini, ChatGPT) got its own version → comparative learning
- The "74" series seems to be college course extra-credit work

---

## Interesting Patterns

**The "Bucky" repos** (3BuckyPrintOnScreen, 4CPPVariables, etc.) are almost certainly from **Bucky Roberts' thenewboston C++ tutorials** on YouTube. The naming convention is a direct reference.

**Multi-AI comparison** — Adam has parallel repos (73CPPChatGPT, 73CPPGrok, 73CPPClaudeOpus, 73CPPGrok) where each AI writes the same lesson's code differently. This is a clever experiment: compare how different AI models approach the same programming problem.

**Heavy annotation style** — Code is extensively commented line-by-line. For example, the `addNumbers` function has a comment block above it explaining what every line does. This is clearly a **learning-first approach** — Adam writes notes *in the code* to retain understanding.

**Naming conveys the concept** — Variable names like `tuna = 99` (then reassign `tuna = 76`) make the code memorable and the concept stick. The comment "Whatever value is stored in a memory location it will overwrite that variable" is more helpful than a dry textbook definition.

**Visual Studio on Windows** — The `.sln`, `.vcxproj`, `vc143.idb` files and `x64/Debug/` structure confirm these are built with Visual Studio on Windows. The M4 Mac mini is likely running Windows via Boot Camp or a VM for the C++ coursework.

**AI-generated variants** — repos like `1.1ClaudeCPP` vs `1.1Oppie1CPP` appear to be the same lesson structure but with code generated by different AI assistants (Claude vs. presumably a local model or custom persona).

---

## What This Reveals About Adam's Learning Approach

- Follows a **structured video course** (likely thenewboston) as the backbone
- Uses **AI as a coding tutor** — generates variations, gets explanations
- Builds **everything locally** and commits complete working projects
- Reviews concepts by **writing explanatory comments** in the code itself
- Uses **comparative AI learning** — same problem, different AI, see the differences

---

*Studied via GitHub API — repo structure from api.github.com, source decoded from base64 blobs.*
